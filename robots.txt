# Cloudflare prepends a managed robots.txt block above this content, including
# its own "User-agent: *" group. A second "User-agent: *" group here would be a
# duplicate that not every crawler merges correctly, so this file carries only
# the sitemap reference, which is group-independent.

Sitemap: https://hofac.org/sitemap.xml
